using UnityEngine;

public class Define
{
    public enum SkillCategory
    {
        None,
        Passive,
        Active,
    }
    public enum DeBuff
    {
        Slow,
    }
    public enum SkillType
    {
        Projectile,
    }
    public enum MoveType
    {
        Targeting,
        Straight
    }
}
